so, part of agent refactor is done. 
i did refactor the Reply part, but didn't touch the main point of this refactor - Event.

it works, but it's still not what I want to see. not how i envisioned it.

but for now it works though.

i understood that there probably is gonna be another module of agent or something like that, that is gonna be responsible for just parsing args and calling agent w/ them.

the overall idea is:

```
┌ runner    :: takes args (python file.py run service action {optional --text}, {(vision)optional --reply_to}), prints back results
│
├ agent     :: executes, constructs Event (with --reply-to - Reply), sends to bunker 
│
└ bunker    :: logs (if Reply -> sends to bot for example)
```
the thing i really fear is.. bunker. this refactor introduces a lot of possibilities. like, a lot of can be improved and implemented.

i also don't like how i don't have a logging system, like, how it looks. in agent i already do different types of logs. with that I will have to come up.
cuz they (my vision) should have the same structure and look good.
