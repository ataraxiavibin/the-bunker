So, I did the bunker thing. It really is barebones and nothing special, literally just a function that returns a new ProcessedReply Pydantic model.

It will be more when there'll be a real Bunker database, but right now we can't do much with the data that get's left out.

The only thing left are: the bot - refactor the code (rn I just repeat the code in all functions), services - adr 01.
