So, long time no see. I actually wanted to polish things up, but I kinda got driven away.

I hate the logging right now. Utterly hate it. I created it, I want to kill it. Destroy it.
1:
`[ERROR] {reason} | SERVICE: {self.service}, ACTION: {self.action}, REPLY_TO: {self.call}`
2:
`successfull execution of {self.service}: {self.action}`
3:
`Processing reply: {reply}`
Yeah, that's straight from the codebase. What a fucking shame. And this is only the start of it.

The problemes are: it doesn't look good, it's messy, it doesn't follow any type of logic. I like it when logs look fancy and coherent. Sue me.

The first problem - looks:

I already have something beautiful. CLI/TTY Telegram bot stylistic. I like it a lot:

    the most important: lower case everywhere.

    `time :: [loglevel] :: {id} :: module :: message`

    `^_>` for starting, sending etc.
    `~~` for results.

The second problem - consistency:

It's pretty much self-explanatory:

    every module, every part of the system will follow this log system,
    and every log-statement will have the same look.

    it will no longer occur, that logs have different "styles"

The third problem - coherency:

    actually is only a part of those two. so it's solved.


