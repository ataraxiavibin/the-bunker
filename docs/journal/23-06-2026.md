*I'll add a little bit of context to that: I always liked Unix philosophy (I mean, I am sucker for Linux), and wanted to do something with it. And at the start of this project, I just wanted add it. So I said: "Services will follow Unix philosophy". To be honest, I didn't even fully understand what it means, and I am sure that even right now I do not understand even 30% of it. Feel free to judge me, I am illiterate; that I acknowledge.*
*And while already building this project, I understood: services are not even close to follow it. I mean, it's just two different things, they don't go together. But I'm still inspired by it, and I think this essentialism is what makes it real good.*

Unix Philosophy stuff:
- It just doesn't pass here, and I'm just not a good enough engineer to even get close to that.
- But, it shows me: 
  - services should probably not do a lot of stuff. just stick to simplicity.
  - but to be exact:
    - [ ] should they even know what Event is?
      - or is it Agent's responsibility to construct it? I think that would make things complex.
      - I could do it so that every service uses shared module that spits out the json if needed.
      - nope.. sucks: the system *must* be language-agnostic.

      - it's possible to make Agent the only power on the device; because Event was a first model, designed to answer the question "how to transport info from service to Bunker, when manually executed?"
      - I already have everything for agent to run the services - path, cmd, args. so, what if agent IS the first entry (to execute manually)?
        - this solves the "info transportation" problem.
      - then, every script is runned by agent and by default prints json and returns a right returncode. 
      - Agent takes it and constructs new Event model (that I also have to think of).


... so, to sum it all up: 
- make services dumb: they return json + returnscode (0 or 1).
- make agents powerful: they run the services (both by call and by manual run), build the pydantic models.
- make bunker smarter: it analyses the context, deciding if to add "warning" flag.
