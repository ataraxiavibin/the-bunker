Ok, so today to the Event refactor.

The problems with Event now are:
  - there is no real reason to check stderr since: if it's there, it's alright if you don't check it. cuz I have much less control over the stderr output, I don't know what can be there.
  is it useful? is it there, because everything broke down? dunno man.

So, we're back to dev mode - everything is gonna be broken today, cuz imma be rewriting everything.

Ok, so let's go with the early models of everything:

Payload (stdout) of every service is just:
 - Dict[str, Any]
 meaning, there could be anything in it. it doesn't say it's name, doesn't say the status. because Agent is the only one who launches the services, it'll know the name. status will be deprecated to just a return code, and Agent will use it to construct the right model. status is a bad idea, because on this basis we can just have two different ok/error models.
 (if there are any unprocessed fields, I am thinking: make bunker process it and add a warning label to Event/Reply model).

 And here is the big point: depending on the return code, two models:

```py
class EventOk(BaseModel):
    service: str
    action: str
    timestamp: datetime
    duration_ms: int
    payload: Dict[str, Any] # idrk what else


class EventError(BaseModel):
    service: str
    action: str
    timestamp: datetime
    duration_ms: int
    exit_code: int
    stderr: str
    reason: Optional[str] = None
    payload: Optional[Dict[str, Any]] = None
```
as I was thinking, it clicked: we can have more info like duration and time in UTC, because everything is ran by agent. prima!

so, (because of it...) right now it looks clamped and i don't like when it's like that lmao. could add something like DiagnosticsOk/Error.

and oh yeah, status is not deprecated. lmao. cuz it's still needed for fatal status... but that looks just stupid: in EventOk to store "status: 'ok'". no? i think so.
it would look better if we use inheritance though.

```py
class EventBase(BaseModel):
    service: str                            # service name, basic stuff
    action: str                             # function called, same here
    stderr: str                             # always get stderr (possible warnings in Bunker)
    timestamp: datetime                     # UTC time, recorded by agent
    duration_ms: int                        # 

class EventOk(EventBase):
    status: Literal["ok"] = "ok"
    payload: Dict[str, Any]                 # stdout is always there (json)

class EventError(EventBase):
    status: Literal["error", "fatal"] = "error"
    exit_code: int
    reason: Optional[str] = None
    payload: Optional[Dict[str, Any]] = None # stdout may not be there (code didn't reach print statement)
```

I also got no idea how am I gonna realise in future the "warning" feature. 
cuz it's either a status change inside Bunker ("agent: ok, bunker: warning! u gotta look here man"; cuz bunker got the context),
or a flag - which kinda pollutes the model. I still want them to be clear.

oh, AI really gave me a good idea: later, when I'll want to add the warning mechanic, just transform the model and return Bunkers model with all the needed info.
seems like AI is not always useless. especially when you have no idea what is possible, but got the idea.
