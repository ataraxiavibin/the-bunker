Well, I finally finished ADR 01 refactor on agent's side.

But there are some moments that are not fully "there" yet: for example, the fact that services still import shared/parser.py.

It's a very convenient way, but to follow the architecture, this must go away.

I'll have to write the arg parsing logic in every service, and although it's nothing - I mean, it's just literally boilerplate - I don't like that.

So, probably gonna make it easier somehow. The first and honestly the only one thing that I can think of is: sys.argv[1]. Right now services are like that: just with one action. Idk if that will ever change, but I think probably yeah, sometime. Cuz services' business logic can be actually complex and it doesn't contradict the adr "stupid services".

But this is enough of future prediction. For now it'll work. I'll switch from `parser` to just `action = sys.argv[1]` sometime soon.
